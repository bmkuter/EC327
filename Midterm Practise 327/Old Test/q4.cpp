#include <string>
#include <iostream>
#include <cstring>
 
using namespace std;

int main(){
    char* nthWord(char "This is a hard midterm", 3){
        int l = strlen(word);
        for (int i = 0; i < l; i++){
            cout << word[i];
        }
        return(0);
    }
}